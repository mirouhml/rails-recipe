<tr>
  <td><%= food.name %></td>
  <td><%= food.measurement_unit %></td>
  <td><%= food.price %></td>
  <td class="del_btn">
    <% if can? :destroy, food %>
      <%= button_to food_path(food), method: :delete, class: "del_btn" do %>
        <span>Delete</span>
      <% end %>
    <% end %>
  </td>
</tr>